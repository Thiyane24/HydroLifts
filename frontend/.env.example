# Em desenvolvimento, o Vite usa /api com proxy para http://localhost:8000
# (definido em vite.config.ts). Em produção, descomenta a linha abaixo com a
# URL pública do backend (sem barra final, sem /api no fim).
VITE_API_URL=https://hydrolifts.onrender.com
